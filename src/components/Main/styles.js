import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  root: {
    width: '100%',
  },

  title: {
    textAlign: 'center',
  },

  subtitle: {
    textAlign: 'center',
  },

  divider: {
    margin: '20px 0',
  },
}));
